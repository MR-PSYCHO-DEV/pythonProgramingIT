Write a Python function that takes two lists and returns True if they have at least one common member.

Write a Python function to return the list of words that are longer than n from a given list of words.
so if it sorts words and returns the works longer than n 

Write a program that takes user input string and converts it to Morse.
(we get given a dict that is copy and pasted in)

Write a Python program to shuffle and print a stored list. Hint, research and use:

from random import shuffle

You may use any other approach instead including the use of the random( ) method.
